# Bookstore Application

A simple Spring Boot application providing CRUD operations for managing books in a bookstore. This project demonstrates typical layers (controller, service, repository) and uses Spring Data JPA with an in‑memory database(h2) for development.

